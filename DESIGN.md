# Further 界面设计契约

> 状态：2026-09 真机评审后重置；首版业务界面基线

完整理由见 [`docs/product/interface-aesthetic-direction.md`](docs/product/interface-aesthetic-direction.md)，
可执行规则见 [`docs/product/interface-visual-system.md`](docs/product/interface-visual-system.md)。

## 核心判断

Further 的个性来自作品、真实记录和安静的使用关系，不来自一套覆盖 iOS 的自定义皮肤。

业务界面采用 Apple 原生设计语言。系统已经提供可靠答案的地方，直接使用原生导航、
按钮、列表、表单、弹层、字体、颜色、图标和转场；只有作品画布、感受色和记录进入
作品这三个与产品本质直接相关的部分，允许建立 Further 自己的视觉语言。

“克制”不是把所有内容变灰、变小或拉开巨大距离，而是减少同时出现的选择、避免
重复容器，并让每个元素只承担一个明确职责。页面可以安静甚至冷淡，但操作必须熟悉、
文案必须直接、状态必须可辨认。

## 不可改变的产品层级

1. 当前作品；
2. 当前页面唯一的主要行动；
3. 感受色与短记；
4. 日期、距离、活动时间和路线；
5. 权限、技术状态和辅助信息。

当前作品是唯一主空间。不使用底部标签栏、数据仪表盘、统计摘要、打卡奖励、训练建议
或社交入口。作品从真实空白开始，沉默是合法表达，更远或更快不能被表现为更好。

## 原生优先规则

- 页面层级使用 `NavigationStack` 和系统导航栏，不自绘顶栏。
- 主要行动使用系统 prominent button；次要行动使用普通按钮、链接或菜单的系统样式。
- 设置和简单选择使用 `Form`、`List`、`Picker`、`Toggle` 等原生组件。
- 临时选择、确认和警告按语义使用 `sheet`、`confirmationDialog` 或 `alert`，不统一套用自定义弹层。
- 图标只使用语义明确的 SF Symbols；图标含义不明确时显示文字。
- 使用系统字体、语义字号、系统背景色、标签色、分隔色和系统强调色，不维护自定义业务界面调色板。
- 保留系统默认按下、禁用、焦点、返回手势、键盘和转场行为，不用自定义动画覆盖它们。
- 不用卡片堆叠、装饰渐变、背景纹理、品牌式大圆角、发光或阴影制造“设计感”。

## 首版视觉范围

- 第一正式版本只设计和验收浅色外观。
- 第一正式版本只设计和验收系统标准文字大小，不单独制作大号字体版式。
- 实现仍使用语义字体，不用固定像素字号封死未来的 Dynamic Type 支持。
- VoiceOver、至少 44×44 pt 的触控区域、非纯颜色状态提示和减弱动态替代仍是首版基础品质。
- 深色模式视觉设计和辅助功能大字号布局作为后续独立阶段，不由 Agent 自行补齐。

## Further 专属设计边界

可以自定义：

- 作品画布及其真实空白、印记、构图、材质和完成形态；
- 感受色的颜色内容及其选择表达；
- 新记录进入作品和作品完成时的因果反馈。

暂未决定的作品美术必须保持开放。业务界面不得用示例颜色、临时印记或装饰材质替它
提前作答，也不得让画布内部内容决定页面导航和操作结构。

## 文案

首版优先使用能够预判结果的直接文案，例如“当前作品”“开始跑步”“作品集”“设置”。
“回望”是已经成立且可理解的领域用词，可以保留。文学性留给作品题名、题签和极少量
观看语境，不用于权限、跑步控制、主要行动和错误恢复。

## 判断标准

当一个设计选择需要被解释为“暮色”“画廊”“慵懒”才能成立时，不采用它。一个合格
页面应当先像一款清楚、可靠、属于 iPhone 的 App；移除画布内容后仍有正确层级，恢复
画布内容后则由作品成为唯一视觉主角。
